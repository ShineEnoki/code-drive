<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">

    <title>Profile</title>

    <style>
        
        .form-container{
            max-width: 400px;
        }
        .card{
            min-width: 300px;
            max-width: 400px;
        }
    </style>
</head>
<body>
    <div class="container mt-5 col-6">

        <!-- profile photo status is store in database-->
        <!-- if photo status is false,this code will show a form to requset a photo for profile -->

        <!-- requesting profile photo -->
        <% if(!photo) { %>
        <div class="form-container">
            <form action="/profile" enctype="multipart/form-data" method="post">
                <input 
                    type="file" 
                    placeholder="Upload profile photo" 
                    name="jpg"
                >
                <input type="submit" placeholder="Upload profile picture">
            </form>
        </div>
        <% } %>
        <!-- end of requesting profile photo -->


        <!-- showing profile data -->
        <div class="card">
            <div class="card-header">
                <h2><%= name %></h2>
            </div>
            <div class="card-body">

                <!-- profile photo status is store in database-->
                <!-- if there is already a profile photo in device 
                this will show profile photo-->
                <% if(photo) { %>
                <div>
                    <img src="../router/photo/<%= name %>.jpg" alt="profile photo" width="200px">
                </div>
                <% } %>


                <div>age:  <%= age %> </div>
                <div> email:  <%= email %> </div>
            </div>
            <div class="card-footer">copyright 2022</div>
        </div>

        <button class="btn btn-outline-primary">
            <a href="http://localhost:8080/logout">Log out</a>
        </button>

        <!-- only admin role can see this button -->
        <% if(role == "admin") { %>
        <button class="btn btn-outline-primary">
            <a href="http://localhost:8080/dashboard">Manage User</a>
        </button>
        <% } %>
        
        
    </div>
</body>
</html>